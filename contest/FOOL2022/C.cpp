#include <algorithm>
#include <bitset>
#include <cmath>
#include <cstdio>
#include <ctime>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <queue>
#include <set>
#include <string>
#include <vector>
#include <iomanip>
// #define TIME

using namespace std;
using LL = long long;
using Pii = pair<int, int>;
using Pll = pair<LL, LL>;

int main() {
  ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  int x;
  cin >> x;
  if (x == 1) {
    cout << "b1 c1 c3 c4";
  } else if (x == 2) {
    cout << "";
  } else if (x == 3) {
    cout << "";
  } else if (x == 4) {
    cout << "";
  } else if (x == 5) {
    cout << "";
  } else if (x == 6) {
    cout << "";
  } else if (x == 7) {
    cout << "";
  } else if (x == 8) {
    cout << "";
  } else if (x == 9) {
    cout << "";
  } else if (x == 10) {
    cout << "";
  } else if (x == 11) {
    cout << "";
  } else {

  }
#ifdef TIME
  fprintf(stderr, "\nTIME: %dms", clock());
#endif
  return 0;
}