#include<bits/stdc++.h>
using namespace std;
int main() {
    for (int i = 1; i <= 1000; i++) {
        system("data.exe"); system("puppet.exe");
        if (system("fc puppet.out puppet.ans")) return 0;
        cout << "Accepted on #" << i << endl;
    }
    return 0;
}