#include <iostream>
#include <algorithm>
#include <vector>
#include <numeric>
#define RF(s) freopen(s".in", "r", stdin), freopen(s".out", "w", stdout)

using namespace std;
using LL = long long;
using Pii = pair<int, int>;
using Pll = pair<LL, LL>;

int main() {
  RF("sakura");
  
  return 0;
}