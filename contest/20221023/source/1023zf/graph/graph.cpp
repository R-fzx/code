#include <bits/stdc++.h>

using namespace std;
using ll = long long;

const int N = 1005;

int n, a[N];

int main() {
	freopen("graph.in", "r", stdin);
	freopen("graph.out", "w", stdout);
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 0;
	return 0;
}
