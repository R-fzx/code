#include <bits/stdc++.h>

using namespace std;

int n, m, a, b, l, r;

int main() {
  freopen("travel.in", "r", stdin);
  freopen("travel.out", "w", stdout);
  cin >> n >> m;
  for (int i = 1; i <= m; i++) {
    cin >> a >> b >> l >> r;
  }
  cout << 0;
  return 0;
}