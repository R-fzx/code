const fs = require('fs')
const path = require('path')
console.log('nice')
const {
    loadWASM,
    OnigScanner,
    OnigString
} = require('.');

const wasmBin = fs.readFileSync(path.join(__dirname, './lib/onigasm.wasm')).buffer
debugger
loadWASM(wasmBin).then(() => {
    const scanner = new OnigScanner(['test'])
    const str = new OnigString('great matest nice')
    const result = scanner.findNextMatchSync(str)
    console.log(result)
})
